name: Service CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  ci:
    uses: SkillCareer/api-gateway/.github/workflows/reusable-ci.yml@main
    with:
      node_version: "18"
      image_name: "api-gateway"
    secrets: inherit
